# Dépendances du POC d'interface Flet (interface_flet.py).
# Séparé de requirements.txt (Tkinter) tant que la bascule n'est pas validée.
# Flet 0.28.3 = dernière version de la lignée d'API « classique » (la 0.80+ est
# une réécriture majeure, à évaluer plus tard).
flet==0.85.3
openpyxl==3.1.5
PyYAML==6.0.3
